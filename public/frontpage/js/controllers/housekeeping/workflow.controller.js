app.controller('WorkflowController', function($scope, $rootScope, $http, $window, $uibModal, $timeout, toaster) {
    var MESSAGE_TITLE = 'Guest Page';

    $scope.tab_full_height = 'height: ' + ($window.innerHeight - 150) + 'px; overflow-y: auto;';
});
