app.controller('CallEventController', function ($scope, $rootScope, $http, $interval, $uibModal, $window, toaster, hotkeys) {
    var MESSAGE_TITLE = 'Call Logger';

    $scope.full_height = 'height: ' + ($window.innerHeight - 100) + 'px; overflow-y: auto';
    $scope.tab_full_height = 'height: ' + ($window.innerHeight - 120) + 'px; overflow-y: auto';



});
